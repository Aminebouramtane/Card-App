import { createSlice } from "@reduxjs/toolkit";


const AddCard = createSlice(
    {
        name:"AddSlice",
        initialState,
        reducers:{
            AddToCard : ()=>{
                
            }
        }
    }
)

export default AddCard